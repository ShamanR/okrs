# Уведомления: технический долг и несоответствия — TODO

**Дата:** 2026-08-27
**Источник:** исполнение фазы 1a ([план](2026-08-27-notifications-1a-event-bus.md)) — ревью задач и финальное ревью ветки.
**Статус:** ничего из перечисленного не блокирует фазу 1a; каждый пункт осознанно отложен с обоснованием.

Файл существует, потому что рабочий ledger исполнения (`.superpowers/sdd/…`) git-ignored и будет удалён. Здесь — то, что должно пережить.

---

## 1. Обязательно закрыть в фазе 1b

### 1.1. Graceful shutdown в `cmd/server/main.go`

`App.Close` (а значит и `bus.Close`) **никогда не выполняется**. `main` не ловит сигналы: `http.ListenAndServe` либо блокируется навсегда, либо возвращает ошибку в `os.Exit(1)`, а тот пропускает `defer`. Та же дыра у соседнего `defer pool.Close()` — она пре-существующая, не регрессия фазы 1a.

**Почему нельзя тянуть:** в фазе 1b появляется воркер доставки уведомлений, и дренаж очереди при остановке перестаёт быть теоретическим. Сейчас терять нечего — единственный подписчик синхронный.

**Что сделать:** `signal.NotifyContext` на `SIGINT`/`SIGTERM`, `srv.Shutdown(ctx)` вместо голого `ListenAndServe`, порядок остановки — HTTP → шина → пул БД.

### 1.2. ~~Проверить набор маршрутов на CRLF-дереве~~ — ЗАКРЫТО в Task 6 фазы 1b

`TestRoutesGolden` и `TestSpecRouteTableMatchesRouter` научены нормализовать CRLF↔LF, а
`-update-routes` пишет CRLF обратно, поэтому неизменённые строки остаются байт-идентичными.
Проверено конструктивно и в обе стороны: удалённый маршрут по-прежнему роняет golden-тест,
маршрут, пропавший из `specs/070`, — spec-тест, лишняя строка в спеке — тоже. Нормализация
ничего не ослабила: `toLF` схлопывает только `\r\n`, а строки маршрутов `\r` не содержат.

**Побочный результат:** `go test ./...` стал полностью зелёным впервые за всё время работы —
эти два теста падали всегда, на любой ветке, включая `main`.

**Остаток долга:** `toCRLF` в `routes_golden_test.go` жёстко зашивает соглашение о записи.
Если репозиторий позже нормализуется на LF через `.gitattributes` (см. п. 4.3), обновление
golden начнёт возвращать CRLF в файл, которому положено быть в LF. Правится не в две строки:
нужно обобщённо определять и сохранять существующее окончание файла плюс тест на это.

### 1.3. Пропущенный `kr_note_updated` в `specs/020-domain-model.md`

Перечень `action` в разделе ActivityEvent содержит 21 значение, в коде их 22 (`internal/core/domain/models.go`). Спека отстала от кода ещё до этой работы.

**Где чинится:** Task 10 плана 1b (правки спек) — уже прописано, здесь дублируется, чтобы не потерялось.

---

## 2. Мелкий долг: чистить попутно

| Что | Где | Почему отложено |
|---|---|---|
| `activitysvc.Service.Record` (одиночный) не имеет ни одного продового вызова — остались только его собственные тесты | `internal/service/activity/activity.go` | Удаление — churn в ветке, чьё главное утверждение «поведение не изменилось» |
| Мёртвое поле `logger` в usecase периода: присваивается, не читается никогда | `internal/usecase/period/overview.go`, проводка в `internal/http/httpdeps/httpdeps.go` | Единственный читатель был в предупреждении `RecordBatch`, которое заменил `PublishBatch`. Удаление — одна строка, но вне скоупа фазы |
| `servicetest.ActivityRepo`: поле `Events` избыточно рядом с `Recorded`, ни один тест их не различает | `internal/service/servicetest/activity.go` | Следующий читатель гадает, на какое поле опираться. Свести к `Recorded` + `BatchCalls` |
| `FakeBus.Events` — экспортированное поле, читается без мьютекса | `internal/service/servicetest/eventbus.go` | Сегодня безопасно: все читатели последовательны, `-race` чист. Станет ловушкой, если тест фазы 1b начнёт публиковать конкурентно. Лечится аксессором `Snapshot()` |
| `Bus.Close` при `!b.started` возвращает `nil`, не выставив `closed` | `internal/platform/eventbus/bus.go` | На так и не запущенной шине публикация после `Close` выполнит Sync-обработчик инлайн. Сегодня недостижимо: и прод, и тестовый seam зовут `Start` до `Close`, а две неподнятые шины в тестах не закрываются вовсе |
| Слово «Исключение» у карва-аута про `service/activity` | `specs/010-architecture-constraints.md` | По смыслу это уточнение, а не исключение из правила: activity ничего не публикует. Формулировка приглашает будущего автора рассуждать «исключения тут бывают». Лазейку закрывает соседняя фраза «единственным сервисом, знающим форму строки журнала» |
| Осиротевшие CSS-правила `.hci-*` (кнопка, панель, бейдж, ~220 строк, `tracker.css:544-767`) | `web/static/tracker.css` | Колокольчик Health Check-in и его панель удалены из JS в Task 8 фазы 1b, правила остались. Удалить вместе с задачей, которая найдёт новое место для Health Check-in (дизайн-спека §13.2) — иначе новая вёрстка может опереться на них и получить неожиданный каскад |
| `storenotif.ListFilter` пересекает границу обработчика | `internal/http/handlers/api/v1/notifications/handler.go` | Осознанно оставлено: протечка store-типов в handlers — пре-существующая привычка репозитория, и `specs/010` уже числит её долгом отдельным абзацем. Здесь записано, чтобы этот конкретный случай попал в тот же реестр. Курсор, который `specs/010` называет поимённо, из обработчика убран |
| §4.1 дизайн-дока обосновывает `eventbus` как «registry-сейм» | `docs/superpowers/specs/2026-08-26-notifications-design.md` | Формулировка, от которой `specs/010` уже отказалась в пользу «общая межслойная машинерия». Дизайн-док — снимок решения на дату, не source of truth |

---

## 3. Ловушка на будущее: реестр `event.AllKinds()`

Реестр поддерживается руками, поэтому **23-я `Kind`-константа, не внесённая в него, не уронит ни один тест**. Проверено мутацией при ревью. Дыра шире, чем кажется: `internal/core/event/event_test.go` тоже перечисляет 22 структуры руками за литералом `len(all) != 22`, то есть новый тип проходит мимо **обоих** тестов.

Сейчас это безопасно: фазы 1a и 1b новых видов событий не добавляют. Риск возникает в фазе 2 (Slack, почта, вебхуки) или при любом расширении домена.

**Закрывается дешевле, чем казалось.** При планировании я записал, что альтернатива — только codegen; это неверно. Хватит ~15 строк теста в `internal/core/event`, который `go/parser`-ом собирает const-блок `Kind = "…"` из `events.go` и сверяет его с `AllKinds()`. Это закрывает связку «константы ↔ реестр» полностью и без генерации, оставляя незакрытым только «новая структура без новой константы» — а это `TestKindsAreUniqueAndNonEmpty` уже делает неудобным пропустить.

**Когда:** вместе с первым же добавлением нового вида события.

---

## 4. Известные несоответствия спек и кода, не входящие в скоуп уведомлений

### 4.1. `specs/010`, раздел «OSS / SaaS split» описывает нерабочий путь расширения

Сказано, что приватный `okrs-saas` «импортирует `okrs/app`, blank-import'ит пакеты с SaaS-регистрациями, выбирает их по имени в `Config`». Регистрирующие функции (`auth.Register`, `auth.RegisterResolveStrategy`, `entitlements.Register`, `nomembership.Register`) при этом лежат в `internal/`, а `okrs/internal/...` по правилу видимости Go импортируется только кодом внутри модуля `okrs`. Из отдельного модуля вызвать их **нельзя**.

Значит описанный путь расширения работает только для кода внутри того же модуля. Обнаружено при проектировании уведомлений; в фазе 2 каналы уведомлений намеренно строятся **иначе** — публичный пакет-контракт `notifychannel` плюс опция `app.Config.NotificationChannels`, — чтобы требование «расширять каналы из внешнего репозитория» вообще было выполнимо.

**Что делать:** отдельная задача — либо переделать сеймы auth/entitlements/nomembership по образцу каналов, либо переписать раздел спеки под то, как оно работает на самом деле. К уведомлениям отношения не имеет, тянуть в их план не следует.

### 4.2. Шесть файлов переписаны из CRLF в LF

Правки фазы 1a сохранили в LF шесть ранее-CRLF файлов; git вернёт им CRLF при следующем касании. На дифф это не повлияло (изменения содержательные, не переписывание целиком), но при коммите стоит убедиться, что не прилетает whitespace-шум.

---

### 4.3. Нормализация окончаний строк во всём дереве

Рабочее дерево преимущественно CRLF, из-за чего `gofmt -l internal/` выдаёт ~450
пре-существующих файлов и непригоден как гейт, а правки агентов местами сохраняли файлы в LF
(git возвращает им CRLF при следующем касании). Radical fix — `.gitattributes` с
`* text=auto eol=lf` плюс разовая нормализация — затрагивает всё дерево и обязан идти
отдельной задачей, не смешиваясь с функциональными изменениями. После него нужно снять
`toCRLF` из `routes_golden_test.go` (см. остаток долга в п. 1.2).

---

## 5. Наблюдение о покрытии, не долг

Сквозной путь «usecase → шина → журнал → `activity_events`» покрыт **одним** тестом на один action (`comment_resolved`, `internal/http/handlers/api/v1/goals/resolve_test.go`). Остальные 21 action проверены только по частям.

Это признано достаточным осознанно: сборка графа — единый общий факт, и один тест доказывает именно её (что подтверждено фальсификацией — с закомментированным `SubscribeAll` тест краснеет). Расширять его до 22 действий значило бы 22 раза проверять одно и то же.

Чего он **не** покрывает: доставку в async-режиме под реальной шиной. В фазе 1a async-подписчиков нет; в 1b появляется первый. Тестовый роутер уже поднимает шину через `bus.Start` — seam подготовлен.
