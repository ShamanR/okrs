## 1. Схема БД и каталог типов

- [x] 1.1 Добавить миграцию `048_access_request_notification`. `up` пересоздаёт CHECK-ограничения `notifications_type` и `notification_preferences_type` с `access_requested`. `down` удаляет строки этого типа из обеих таблиц и восстанавливает прежние ограничения. Проверка: миграция применяется и откатывается на тестовой БД, тесты `internal/store/...` зелёные.
- [x] 1.2 Дополнить seed demo (`seed_demo.sql`) включённой настройкой `access_requested` у демо-пользователя и одним уведомлением этого типа; `internal/store/seed.go` уведомлений не содержит. Проверка: `go test ./internal/store/...` (включая `seed_test.go`) зелёный.
- [x] 1.3 Сначала написать тесты на каталог типов в `store/notificationprefs`:
  - порядок: «Цели», затем «Системные»;
  - `CategoryOf`;
  - `AudienceOf`;
  - `IsAddressed` для `access_requested`;
  - `TypesFor(false)` отдаёт 4 типа, `TypesFor(true)` — 5;
  - `defaultPreference` для типов «Целей» включён, для `access_requested` выключен.

  Затем реализовать каталог (`Type`, `Category`, `Audience`, `AdminOnly`, `DefaultEnabled`) и константу `TypeAccessRequested`; `AllTypes` и `IsAddressed` вывести из каталога. Проверка: `go test ./internal/store/notificationprefs/...` зелёный.
- [x] 1.4 Сначала написать тест, потом реализовать `Repository.ResolveTenantAdmins(ctx, scope, typ, actorIDs)` одним запросом (см. design §3). Тест проверяет:
  - получают только активные администраторы этого пространства, явно включившие тип;
  - администратор без сохранённой настройки не получает (значение по умолчанию — выключено);
  - автор исключён;
  - администратор с отключённым типом отфильтрован;
  - `Ord` корректно сопоставлен для пачки из нескольких событий;
  - администратор другого пространства не попадает в результат.

  Проверка: тест проходит.

## 2. Доменное событие и публикация из онбординга

- [x] 2.1 Добавить в `core/event` Kind `access_requested`, структуру `AccessRequested{Meta; TenantTitle}` и запись в `AllKinds()`. Добавить образец в тест eventlog, выбрать тег `log` для `TenantTitle` по правилам observability-logging. Проверка: `go test ./internal/platform/eventlog/...` зелёный.
- [x] 2.2 Добавить в `service/activity` явную ветку `toRows` для `AccessRequested`, которая не порождает строк. Добавить запись в `TestToRowCoversEveryEventType` с ожиданием нуля строк. Проверка: `go test ./internal/service/activity/...` зелёный.
- [x] 2.3 Сначала написать тесты в `service/onboarding` на `FakeBus`:
  - новая заявка публикует одно `AccessRequested` с пространством, заявителем и названием пространства;
  - повторная заявка при статусе `requested` ничего не публикует;
  - заявка после отклонения (членства нет) публикует событие;
  - `ErrAlreadyMember` и `ErrTenantNotFound` ничего не публикуют.

  Проверка: до реализации тесты падают.
- [x] 2.4 Добавить в `onboarding.Service` порт `Publisher` через конструктор и публикацию в `RequestAccess` после успешного upsert (design §2). Подключить bus в `internal/http/server.go` и обновить все места вызова `onboarding.New` в тестах. Перед изменением сигнатуры выполнить `change(operation:"verify")`. Проверка: тесты из 2.3 проходят, `go build ./...` и `go test ./internal/http/...` зелёные.

## 3. Конвейер уведомлений

- [x] 3.1 Сначала написать тесты `usecase/notification`, затем реализовать:
  - `AccessRequested` превращается в тип `access_requested` с `EntityTitle` = название пространства и пустыми `TeamID` и `GoalID`;
  - событие не отбрасывается из-за отсутствия команды;
  - получатели берутся из `ResolveTenantAdmins`;
  - строки ленты и `Delivery` формируются для каждого администратора;
  - ключ схлопывания использует сущность `tenant:<id>`.

  Реализация: `mapping.go` (`notifyType`, `anchorOf`), развилка по `AudienceOf` в `Handle`/`resolve`, расширение порта `PrefResolver`. Проверка: `go test ./internal/usecase/notification/...` зелёный.
- [x] 3.2 Сначала написать тесты `render/notify`, затем реализовать формулировку и ссылку:
  - `wording` для `KindAccessRequested`: «Заявка на доступ», в теле — имя заявителя и название пространства;
  - `TargetURL` с `Kind = access_requested` возвращает `/admin?section=users&filter=requests`;
  - ссылки для остальных Kind не меняются.

  Реализация: поле `Kind` в `LinkInput`, передать его в обработчике ленты (`targetURL`) и в `notificationdelivery.render`. Проверка: `go test ./internal/render/... ./internal/usecase/notificationdelivery/... ./internal/http/handlers/api/v1/notifications/...` зелёный.
- [x] 3.3 Сначала написать тесты, затем расширить правило «не бывший» до `status IN ('active','requested')` в `store/notifications.List` и `store/users.ContactsByIDs`. Тесты проверяют:
  - автор с поданной заявкой показан по имени в ленте и в контактах доставки;
  - после удаления заявки он показан как бывший участник;
  - системные пользователи по-прежнему не маскируются.

  Проверка: `go test ./internal/store/notifications/... ./internal/store/users/...` зелёный.
- [x] 3.4 Добавить в `notificationdelivery` тест: для уведомления о заявке сообщение называет заявителя и пространство и содержит абсолютную ссылку на очередь заявок, если адрес продукта настроен, и не содержит ссылки, если адрес не настроен. Проверка: тест проходит.

## 4. Настройки уведомлений

- [x] 4.1 Сначала написать тесты `service/notificationpref`, затем реализовать:
  - `GetAll` для не-администратора отдаёт 4 типа, для администратора — 5 с категориями;
  - без сохранённой настройки `access_requested` отдаётся выключенным, типы «Целей» — включёнными;
  - `SetAll` не-администратора с `access_requested` отклоняется с `ErrInvalidType` и ничего не сохраняет;
  - сохранённая настройка бывшего администратора читается снова, когда роль возвращается.

  Реализация: параметр `isAdmin` в `GetAll`/`SetAll`, перед этим `change(operation:"verify")`. Проверка: `go test ./internal/service/notificationpref/...` зелёный.
- [x] 4.2 Обновить обработчик `api/v1/notifications/preferences`:
  - `isAdmin` берётся из роли членства принципала в активном пространстве;
  - в DTO элемента добавляется поле `category`.

  Тесты:
  - заменить `TestGetReturnsAllFourTypes` тестами для участника (4 типа, категория `goals`) и администратора (5 типов, `access_requested` — `system` и `addressed`);
  - PUT участника с `access_requested` отвечает 400.

  Проверка: `go test ./internal/http/handlers/api/v1/notifications/preferences/...` зелёный.

## 5. Интерфейс

- [x] 5.1 Реализовать в `web/static/settings.js`:
  - `NOTIF_CATEGORIES` (`goals` — «Цели», `system` — «Системные»);
  - строки-заголовки групп в таблице `NotificationsSettings`, пустые категории не выводятся;
  - подпись и подсказку для `access_requested`;
  - упоминание системных уведомлений во вводном тексте секции.

  Добавить стиль `notif-prefs__group` в общий CSS настроек. Проверка: в запущенном приложении администратор видит две группы, участник — одну; сохранение матрицы работает.
- [x] 5.2 В `web/static/admin.js` `UsersSection` должен брать начальный фильтр из `?filter=requests`. Проверка: `/admin?section=users&filter=requests` открывает раздел с активным чипом «Заявки».
- [x] 5.3 Проверить, что клик по уведомлению в колокольчике переходит по `url` из ответа. Если клиент собирает ссылку из полей цели, перевести его на `url`. Проверка: в запущенном приложении клик по уведомлению о заявке открывает очередь заявок, а клик по уведомлению о цели по-прежнему открывает цель.

## 6. Документация и итоговая проверка

- [x] 6.1 Обновить раздел Notifications в `README.md`: администратор получает уведомление о заявках на доступ, в настройках есть категории «Цели» и «Системные». Проверка: текст README соответствует поведению.
- [x] 6.2 Прогнать `go build ./...`, `go vet ./...` и `go test ./...`. Сквозная проверка в запущенном приложении: пока администратор не включил тип, заявка уведомления не порождает; после включения типа пользователь подаёт заявку по slug, администратор видит уведомление в колокольчике и получает сообщение в Mattermost (или в проверочном канале), а повторная заявка нового уведомления не порождает. Проверка: все команды зелёные, сценарий воспроизводится.
- [x] 6.3 Сверить реализацию с delta specs (`openspec validate add-access-request-notification --strict`) и провести code review изменений. Проверка: валидация проходит, замечания ревью закрыты.

## 7. Правки по code review

- [x] 7.1 Сузить исключение «заявитель назван по имени» до строк типа `access_requested`: в `store/notifications.List` признак вычисляется с учётом типа строки, в `notificationdelivery.render` — с учётом `Kind`. Тесты: бывший участник с поданной заявкой назван в уведомлении о заявке и скрыт в своих прежних уведомлениях (лента и внешний канал). Проверка: `go test ./internal/store/notifications/... ./internal/usecase/notificationdelivery/...` зелёный.
- [x] 7.2 Сделать создание заявки атомарным: `memberships.CreateRequest` (`INSERT … ON CONFLICT DO NOTHING`) сообщает, была ли вставка, и событие публикуется только при ней. Тесты: повторный вызов не вставляет и не публикует; действующее членство не понижается до заявки; одобрение, отклонение и отмена не публикуют; заявка после отмены публикуется. Проверка: `go test ./internal/store/memberships/... ./internal/service/onboarding/...` зелёный.
- [x] 7.3 Заменить в seed demo системного пользователя на демо-заявителя с поданной заявкой, чтобы демо показывало правило именования. Проверка: `seed_demo.sql` дважды применяется на чистой БД, заявитель виден в очереди «Заявки».
- [x] 7.4 Добавить переход `GET /open?tenant=&to=` (`handlers/web/openlink`): делает пространство уведомления активным и ведёт по внутреннему пути; при неактивном членстве и при внешнем адресе — на `/`. Ссылки внешних каналов (все типы, включая цели) строятся через него: `notify.LinkInput.TenantID` задаёт доставка. Обновить golden маршрутов. Проверка: `go test ./internal/http/... ./internal/render/... ./internal/usecase/notificationdelivery/...` зелёный.
