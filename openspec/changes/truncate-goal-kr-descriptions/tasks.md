## 1. Общий компонент сворачивания описания

- [x] 1.1 В `web/static/markdown.js` рядом с `Markdown` добавить компонент
      `CollapsibleMarkdown({ text, className })`, который рендерит тот же санитизированный
      HTML, что и `Markdown` (через `renderMarkdown`), но оборачивает его в контейнер
      сворачивания; при пустом тексте, как и `Markdown`, возвращать `null`. Проверить: на
      странице доски описание цели по-прежнему рендерится с разметкой, консоль браузера без
      ошибок.
- [x] 1.2 Реализовать в `CollapsibleMarkdown` локальное состояние развёрнутости
      (`useState(false)`), сбрасываемое при изменении `text` (решение 4 в `design.md`).
      Проверить: после правки описания цели через редактор описание снова отображается
      свёрнутым.
- [x] 1.3 Реализовать определение переполнения: измерение `scrollHeight` против
      `clientHeight` в `useLayoutEffect` после вставки HTML и пересчёт через
      `ResizeObserver` на контейнере, с однократным измерением при отсутствии
      `ResizeObserver` (решение 3 в `design.md`). Проверить: кнопка появляется у длинного
      описания, отсутствует у короткого, и появляется/исчезает при изменении ширины окна.
- [x] 1.4 Добавить кнопку разворачивания/сворачивания под описанием: `<button>` с
      `aria-expanded`, текстом «Показать полностью» / «Свернуть», с `stopPropagation` на
      клике и подавлением старта перетаскивания (решение 5 в `design.md`). Проверить: клик по
      кнопке на карточке цели не открывает редактор цели и не начинает перетаскивание
      карточки или строки KR.

## 2. Стили сворачивания

- [x] 2.1 В `web/static/markdown.css` добавить стили контейнера сворачивания: `max-height`
      из CSS-переменной, `overflow: hidden`, градиентная накладка от прозрачного к цвету
      фона через CSS-переменную, стили кнопки. Не задавать в `markdown.css` `font-size`,
      `color` и `line-height` — правило host-типографики сохраняется. Проверить: длинное
      описание визуально обрезано с затенением, короткое — без затенения.
- [x] 2.2 В `web/static/tracker.css` задать лимиты на host-классах: `.goal-card__desc` —
      4 строки, `.kr-desc` — 3 строки, выраженные в `em` кратно действующему `line-height`
      (1.5 и 1.4 соответственно). Проверить: описание цели в свёрнутом виде занимает
      4 строки, описание KR — 3 строки, граница обрезания проходит по строке текста.
- [x] 2.3 Задать цвет фона градиента для состояний карточки цели (`goal-card--stale`,
      `goal-card--shared`, `goal-card--dragging`) и для строки KR так, чтобы накладка
      совпадала с фактическим фоном. Проверить визуально: у устаревшей и у общей цели
      затенение не читается как посторонняя полоса.
- [x] 2.4 Добавить в `markdown.css` блок `@media print`, снимающий ограничение высоты и
      скрывающий градиент и кнопку (решение 6 в `design.md`). Проверить в предпросмотре
      печати доски: описания целей и KR печатаются полностью.

## 3. Подключение на доске команды

- [x] 3.1 В `web/static/tracker.js` в `GoalCard` заменить рендер описания цели
      (`<Markdown text={goal.desc} className="goal-card__desc" />`) на `CollapsibleMarkdown`
      с тем же классом. Проверить сценарии «Длинное описание цели свёрнуто», «Короткое
      описание не обрезается» и «Цель без описания» из delta spec.
- [x] 3.2 В `web/static/tracker.js` в `KRRow` заменить рендер описания KR
      (`<Markdown text={kr.desc} className="kr-desc" />`) на `CollapsibleMarkdown` с тем же
      классом. Проверить сценарий «Длинное описание ключевого результата свёрнуто» при
      раскрытом блоке KR.
- [x] 3.3 Убедиться, что остальные места рендера описаний остались на `Markdown`: окно
      обновления прогресса KR (`kr-progress-desc__text`), описание команды в шапке
      (`topbar__desc`), раздел описаний в `web/static/settings.js` (`desc-row__desc`).
      Проверить сценарий «Окно обновления прогресса» из delta spec.

## 4. Проверка поведения по delta spec

- [x] 4.1 Проверить сценарии «Разворачивание описания», «Сворачивание обратно» и
      «Разворачивание не затрагивает соседние описания»: на доске с несколькими длинными
      целями развернуть одну — остальные остаются свёрнутыми, повторный клик сворачивает.
- [x] 4.2 Проверить сценарий «Разметка видимой части сохраняется» на описании с заголовками
      и маркированными списками (пример из `proposal.md` — «Проблема:» со списком и
      «Последствия»): в свёрнутом виде видимая часть сохраняет заголовки и маркеры списка.
- [x] 4.3 Проверить сценарии «Перезагрузка страницы» и «Смена команды или периода»: после
      перезагрузки и после возврата на команду/период все длинные описания снова свёрнуты;
      убедиться, что в `localStorage` не появилось новых ключей.
- [x] 4.4 Проверить сценарий «Разворачивание не открывает редактор» в режиме
      `editMode === 'full'` и корректность отображения в режиме только для чтения.
- [x] 4.5 Проверить сценарий «Экспорт цели»: markdown-экспорт цели с длинным описанием
      содержит полный текст описания.
- [x] 4.6 Прогнать `go build ./...` и `go test ./...` — серверная часть не затрагивается,
      регрессий быть не должно.

## 5. Документация

- [x] 5.1 Дополнить README в разделе про Markdown (описание про goal и KR descriptions,
      строки 289–291) одним предложением о том, что длинные описания целей и KR на доске
      показываются свёрнутыми и разворачиваются по кнопке. Проверить: README остаётся
      консистентным, ссылки и скриншоты не сломаны.
