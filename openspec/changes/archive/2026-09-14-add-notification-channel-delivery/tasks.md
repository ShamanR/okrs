## 1. Контракт канала

- [x] 1.1 Ввести в `notifychannel` структуру зависимостей канала (настройки,
  логгер `*slog.Logger`, источник времени) и перевести `Channel.New` на неё;
  проверка — пакет собирается и в нём по-прежнему нет импортов `okrs/internal/**`
  (тест, перечисляющий импорты пакета, падает при появлении внутреннего импорта)
- [x] 1.2 Расширить `notifychannel.Sender` методами немедленной отправки и выгрузки
  накопленного, задокументировав новый смысл обычной отправки («принять
  к отправке»); проверка — тесты пакета `notifychannel` проходят, компилятор
  указывает все места, требующие обновления
- [x] 1.3 Обновить существующие тестовые заглушки `Sender` под новый интерфейс;
  проверка — `go build ./...` и `go vet ./...` проходят

## 2. Mattermost: накопление и отправка

- [x] 2.1 Добавить в дескриптор канала поле окна отправки с значением по
  умолчанию в десять минут и валидацией положительного числа; проверка — тест
  на отклонение нулевого, отрицательного и нечислового значения
- [x] 2.2 Реализовать накопление обновлений по получателям и отправку по
  истечении окна одним сообщением; проверка — тест с управляемым временем:
  три обновления за окно дают ровно один исходящий запрос
- [x] 2.3 Реализовать формирование текста накопленного сообщения из нескольких
  обновлений; проверка — тест на содержимое: в сообщении присутствуют все
  накопленные обновления и счётчики повторов
- [x] 2.4 Реализовать немедленную отправку, минующую накопление и возвращающую
  ошибку вызывающему; проверка — тест: немедленная отправка делает запрос сразу
  и не затрагивает накопленное других получателей
- [x] 2.5 Реализовать выгрузку накопленного по требованию; проверка — тест:
  после выгрузки накопленное пусто и отправлено одним сообщением
- [x] 2.6 Реализовать потолок накопленного на получателя с отбрасыванием самых
  старых обновлений; проверка — тест: при переполнении новые обновления
  принимаются, старые отбрасываются, накопленное другого получателя не затронуто
- [x] 2.7 Перевести ошибки доставки на инжектированный логгер; проверка — тест:
  при отказе внешнего сервиса запись появляется в логгере, а обычная отправка
  возвращает `nil`
- [x] 2.8 Проверить, что кэш идентификатора бота переиспользуется в пределах
  экземпляра канала; проверка — тест: несколько отправок дают один запрос
  разрешения идентификатора

## 3. Ядро: реестр экземпляров и защита секрета

- [x] 3.1 Переопределить немедленную отправку в маскирующей обёртке, чтобы
  секрет вырезался и из её ошибки; проверка — тест: ошибка немедленной отправки,
  содержащая секрет в адресе, возвращается без значения секрета
- [x] 3.2 Обновить комментарий у маскирующей обёртки — утверждение «кроме
  отправки методов нет» стало неверным; проверка — комментарий описывает
  фактический состав интерфейса и причину переопределения обоих методов
- [x] 3.3 Реализовать редактирующий логгер: обработчик, вырезающий секрет
  пространства, передаётся каналу при построении; проверка — тест: запись,
  содержащая секрет, выходит из обработчика без него
- [x] 3.4 Реализовать реестр экземпляров каналов `(пространство, канал)` с
  построением один раз; проверка — тест: повторное получение канала возвращает
  тот же экземпляр
- [x] 3.5 Реализовать сверку версии конфигурации и пересоздание экземпляра с
  выгрузкой накопленного перед заменой; проверка — тест: после изменения
  настроек накопленное отправлено по прежней конфигурации, новый экземпляр
  построен по новой
- [x] 3.6 Перевести обработчик проверочной отправки на немедленную отправку;
  проверка — существующие тесты проверочной отправки проходят, добавлен тест:
  неверный секрет даёт ошибку, а не успех
- [x] 3.7 Подключить выгрузку накопленного всех живых экземпляров к штатной
  остановке приложения, с ограничением по времени; проверка — тест: при
  остановке накопленное отправляется, а остановка не ждёт дольше лимита

## 4. Хранение настроек и структура базы

- [x] 4.1 Миграция настроек уведомлений: разреженное хранение отклонений по
  каналам, перенос существующего значения канала «в приложении» как явного
  выбора; проверка — тест миграции на данных до и после
- [x] 4.2 Миграция настроек канала: признак «включён по умолчанию» (выключен для
  существующих строк). Окно отправки колонки не требует — по design.md это
  обычное поле канала (`Descriptor.Fields`), которое едет в `config_json`;
  проверка — тест: после миграции поведение существующих пространств не меняется
- [x] 4.3 Обновить seed demo под обе миграции; проверка — накат seed на пустую
  базу проходит, демо-пространство содержит согласованные настройки
- [x] 4.4 Реализовать в резолве получателей трёхзначную логику «явно включено /
  явно выключено / не высказывался» с подстановкой значения администратора;
  проверка — тесты на все три состояния и на ретроактивность в обе стороны
- [x] 4.5 Убедиться, что резолв остался пакетным и не добавил запросов;
  проверка — существующий тест на пакетность расширен на каналы и проходит

## 5. Доставка

- [x] 5.1 Объявить в `internal/usecase/notification` узкий порт доставки на
  стороне потребителя; проверка — пакет usecase не импортирует `notifychannel`
- [x] 5.2 Реализовать порт в слое сервиса: выбор каналов получателя, разрешение
  адреса и передача сообщения каналу; проверка — тест: уведомление попадает
  только в каналы, включённые у получателя
- [x] 5.3 Реализовать пакетное разрешение адресов получателей одним запросом;
  проверка — тест: пачка из нескольких получателей даёт один запрос адресов
- [x] 5.4 Перестать отбрасывать список каналов получателя в обработчике шины
  и вызывать доставку; проверка — тест: событие доходит до канала-заглушки
- [x] 5.5 Реализовать создание записи уведомления независимо от каналов и
  скрытие уведомления из ленты и счётчика при отключённом канале «в приложении»;
  проверка — тест: запись создана, в ленте отсутствует, во внешний канал ушла
- [x] 5.6 Проверить, что неудачная доставка не меняет запись уведомления;
  проверка — тест: при отказе канала лента и счётчик непрочитанного не изменились

## 6. API

- [x] 6.1 Добавить в настройки канала признак «включён по умолчанию» и окно
  отправки, с валидацией окна; проверка — тесты обработчика на сохранение,
  чтение и отклонение недопустимого окна
- [x] 6.2 Перевести настройки уведомлений пользователя на матрицу «тип × канал»
  с отдачей всех доступных пространству каналов; проверка — тесты обработчика:
  ответ содержит все каналы, положение по умолчанию берётся из настройки
  администратора
- [x] 6.3 Реализовать сохранение матрицы как отклонений от текущего значения по
  умолчанию; проверка — тест: ячейка, совпавшая с умолчанием, не сохраняется как
  отклонение, а отличающаяся — сохраняется
- [x] 6.4 Отклонять неизвестный канал в настройках как ошибку валидации;
  проверка — тест обработчика на недоступный пространству канал

## 7. Интерфейс

- [x] 7.1 Добавить на экран настроек канала администратора признак «включён по
  умолчанию у всех» с пояснением, что сотрудник может отключить его сам, и поле
  окна отправки; проверка — признак и поле сохраняются и отображаются после
  перезагрузки страницы
- [x] 7.2 Развести формулировки «канал активен» и «включён по умолчанию у всех»
  так, чтобы они не читались как одно и то же; проверка — обе подписи различимы
  на экране, вторая недоступна, пока канал выключен
- [x] 7.3 Перевести экран настроек уведомлений пользователя на матрицу
  «тип × канал», переиспользуя существующие общие элементы управления;
  проверка — матрица отображает все доступные каналы, переключение и сохранение
  работают
- [x] 7.4 Проверить читаемость матрицы при нескольких каналах и четырёх типах;
  проверка — экран остаётся пригодным для использования на узком экране

## 8. Сквозная проверка

- [x] 8.1 Сквозной тест: администратор настраивает канал и включает его по
  умолчанию, происходит несколько событий, получатель получает одно сообщение
  со всеми обновлениями
- [x] 8.2 Сквозной тест: получатель отключает один тип в одном канале, остальные
  типы и остальные каналы продолжают работать
- [x] 8.3 Сквозной тест: канал подключён после того, как пользователь сохранял
  настройки, и доезжает до него без его действий
- [x] 8.4 Проверить соответствие реализации delta-спекам этого change и
  запустить полный набор тестов проекта; проверка — `go test ./...` проходит

## 9. Кликабельная ссылка в доставленном сообщении

Найдено на первом же реально доставленном сообщении: ссылка приходила
относительной и в мессенджере не открывалась.

- [x] 9.1 Передать собственный адрес развёртывания в слой доставки и строить
  абсолютную ссылку; проверка — тест: ссылка начинается с настроенного адреса,
  завершающий слэш не даёт двойного
- [x] 9.2 Не отправлять ссылку вовсе, когда адрес не настроен, и предупредить об
  этом один раз за процесс; проверка — тест: сообщение уходит без ссылки
- [x] 9.3 Отправлять ссылку разметкой канала, а не голым адресом; проверка —
  тест: в посте Mattermost ссылка оформлена как markdown-ссылка
