## 1. Детерминированная привязка цели к команде

- [x] 1.1 В `computePeriodOverview` (`internal/usecase/period/overview.go`) заменить обход
      `teamsByID` по map на обход по отсортированному возрастанию срезу идентификаторов
      команд; проверить `go test ./internal/usecase/period/...` — существующие тесты обзора
      проходят без изменений.
- [x] 1.2 Реализовать выбор команды цели по правилу из `design.md` (решение 1):
      команда-владелец `domain.Goal.TeamID`, если она в охвате, иначе первая по возрастанию
      идентификатора команда охвата с этой целью; заполнять этой командой `TeamID`/`TeamName`
      в `PeriodGoalItem`.
- [x] 1.3 Добавить в `internal/usecase/period/overview_test.go` тест: общая цель, владелец
      которой в охвате, привязана к владельцу, а не к команде-участнику; проверить, что тест
      падает до правки 1.2 и проходит после.
- [x] 1.4 Добавить тест: общая цель, владелец которой вне охвата (teamFilter без владельца),
      привязана к команде из охвата и результат не зависит от повторного вызова; проверить
      `go test ./internal/usecase/period/...`.

## 2. Идентификаторы для перехода к ключевому результату

- [x] 2.1 Добавить в `PeriodKRItem` поля `GoalID int64` (`json:"goal_id"`) и `TeamID int64`
      (`json:"team_id"`); проверить `go build ./...`.
- [x] 2.2 Заполнять `GoalID` идентификатором цели, а `TeamID` — той же командой, что выбрана
      для цели в задаче 1.2, в том же проходе цикла без дополнительных обращений к базе
      данных.
- [x] 2.3 Расширить `TestComputePeriodOverview_HealthBalanceAndKRList` проверкой, что у
      каждого ключевого результата заполнены `GoalID` и `TeamID` и что `TeamID` совпадает с
      `TeamID` его цели в `Goals`; проверить `go test ./internal/usecase/period/...`.
- [x] 2.4 Дополнить тест обработчика в
      `internal/http/handlers/api/v1/periods/overview/handler_test.go` проверкой, что
      `goal_id` и `team_id` присутствуют в JSON-ответе обзора; проверить
      `go test ./internal/http/...`.

## 3. Переходы в представлении обзора

- [x] 3.1 В `web/static/period_overview_view.js` добавить локальный компонент строки состава,
      который при наличии адреса рендерит `<a href target="_blank" rel="noopener">`, а при
      его отсутствии — прежний текст; адрес строится через существующий `buildTargetURL` из
      `web/static/ui.js`.
- [x] 3.2 Перевести список команд (drill-down плиток статусов, «Команды с целями», «Ошибки
      весов») на этот компонент с целью перехода `{team_id: t.id, period_id: data.period_id}`;
      проверить в браузере: клик по строке открывает доску этой команды отдельно от обзора,
      обзор в исходном окне не заменяется.
- [x] 3.3 Перевести список целей (drill-down балансов) на тот же компонент с целью перехода
      `{team_id: g.team_id, period_id: data.period_id, goal_id: g.id}`; проверить в браузере,
      что открывается доска нужной команды с раскрытой целью.
- [x] 3.4 Перевести список ключевых результатов (drill-down баланса health-статусов) на тот
      же компонент с целью перехода
      `{team_id: kr.team_id, period_id: data.period_id, goal_id: kr.goal_id, kr_id: kr.id}`;
      проверить в браузере, что открывается доска с раскрытой целью и выделенным ключевым
      результатом.
- [x] 3.5 Проверить, что правая часть строки (прогресс, сумма весов, признак ошибки весов) и
      обрезка длинного названия по ширине сохранились во всех трёх списках без визуальных
      регрессий.

## 4. Проверка обоих потребителей и соответствия спецификации

- [x] 4.1 Открыть страницу `/period-overview` и убедиться, что переходы работают во всех трёх
      списках в охвате «мои команды» и, под администратором, в охвате «вся организация».
- [x] 4.2 Открыть модальное окно обзора периода в администрировании
      (`PeriodOverviewModal`) и убедиться, что переходы ведут к тем же сущностям и модальное
      окно остаётся открытым; шаблоны не менять — `ui.js` уже подключён обоими shell.
- [x] 4.3 Прогнать `go build ./... && go test ./...` и убедиться, что весь набор проходит.
- [x] 4.4 Сверить реализацию со сценариями `specs/period-overview/spec.md`: каждый сценарий
      подтверждён тестом или воспроизведён вручную.

## 5. Уборка

- [x] 5.1 Согласовать с пользователем удаление пустой заготовки изменения
      `openspec/changes/add-overview-drilldown-deep-links` (каталог `specs/period-overview`
      без файлов, не в git); удалить только после подтверждения.
