## 1. Пакет logging: схема, формат, редакция

- [x] 1.1 Создать пакет `internal/platform/logging` с конфигурацией
  (уровень, формат, имя сервиса, окружение) и функцией сборки `*slog.Logger`;
  проверить unit-тестом, что при формате `json` вывод является валидным
  JSON-объектом в одну строку, а при `text` — текстовым
- [x] 1.2 Реализовать разбор уровня логирования из строкового значения
  с падением на `info` при недопустимом значении и предупреждающей записью;
  проверить тестами на `debug`/`info`/`warn`/`error`, пустое и мусорное значение
- [x] 1.3 Добавить реестр констант поля `event`
  (`http_request`, `http_panic`, `domain_event`, `auth_login`, `auth_logout`,
  `auth_failed`, `authz_denied`, `access_changed`, `app_start`, `app_ready`,
  `app_shutdown`, `migration`, `background_task`, `external_call`,
  `event_dropped`, `config_invalid`); проверить тестом, что все константы
  уникальны
- [x] 1.4 Реализовать `ReplaceAttr` с денайлистом ключей (`password`, `token`,
  `secret`, `authorization`, `cookie`, `api_key`, `email`, `webhook_url`
  и производные) и заменой значения на фиксированную маску; проверить тестом,
  что исходные значения отсутствуют в выводе, а сопоставление ключа
  нечувствительно к регистру и разделителям
- [x] 1.5 Реализовать `contextHandler`, добавляющий в запись `request_id`,
  `tenant_id`, `actor_id`, `team_id`, `period_id` из `context`, вместе
  с хелперами `WithRequestID`/`WithScope`; проверить тестом, что поля
  появляются при `InfoContext` и отсутствуют (а не подставляются нулями),
  когда контекст пуст
- [x] 1.6 Проверить тестом, что каждая запись содержит обязательные поля
  (время, уровень, сообщение, `event`, `service`, `env`) и что значение
  с переводами строк остаётся внутри одной строки вывода

## 2. Композиция и жизненный цикл

- [x] 2.1 Собрать логгер через `logging` в `cmd/server/main.go`, читая
  `LOG_LEVEL`, `LOG_FORMAT`, `SERVICE_NAME`, `ENV` из окружения (значения
  по умолчанию: `info`, `json`, имя сервиса и `dev`); проверить запуском,
  что вывод стал JSON
- [x] 2.2 Заменить текстовый fallback-логгер в `app.New` на `logging`-сборку
  по умолчанию, не читая окружение внутри `app`; проверить существующим
  тестом сборки `app`, что он проходит без переменных среды
- [x] 2.3 Перевести существующие lifecycle-записи в `cmd/server/main.go`
  на константы `event` и добавить недостающие: старт, результат миграций,
  готовность принимать запросы, получение сигнала остановки, завершение
  обслуживания, завершение обработки отложенных событий, отказ при запуске;
  проверить тестом сценариев жизненного цикла из spec
- [x] 2.4 Добавить логирование запуска, завершения и ошибок фоновых задач
  в `Server.StartBackground`; проверить тестом, что запись содержит имя
  задачи и исход

## 3. HTTP: корреляция, access log, ошибки, паники

- [x] 3.1 Реализовать middleware `RequestID` в `internal/http/middleware`:
  принимать `X-Request-Id` только при прохождении валидации длины и набора
  символов, иначе генерировать новый; класть в `context` и возвращать
  в ответе; проверить тестами на генерацию, наследование, отклонение
  многострочного и переразмерного значения
- [x] 3.2 Перенести `AccessLogMiddleware` и `responseWriter` из
  `internal/auth/middleware.go` в `internal/http/middleware`, сохранив
  поведение; проверить, что сборка проходит и существующие тесты
  `internal/http` зелёные
- [x] 3.3 Расширить обёртку ответа методом `RecordError(code string, cause error)`
  и записывать итоговую запись `http_request` с уровнем по статусу
  (2xx/3xx — `info`, 4xx — `warn`, 5xx — `error`), включая метод, путь
  без строки запроса, статус, длительность, признак аутентифицированности,
  код и причину ошибки; проверить тестами на каждый уровень и на отсутствие
  строки запроса в поле пути
- [x] 3.4 Добавить в оба `WriteError` (`api/v1/errors.go`
  и `admin/admincommon/admincommon.go`) type assertion writer'а на интерфейс
  записи ошибки, не меняя сигнатуру функций; проверить тестом связки
  middleware + обработчик, что ответ 500 порождает ровно одну запись уровня
  `error` с кодом и причиной
- [x] 3.5 Прекратить отдавать `err.Error()` в теле ответа
  `admincommon.WriteError` для 5xx, передавая техническую причину только
  в лог; проверить тестом, что тело ответа не содержит техническую причину,
  а лог — содержит
- [x] 3.6 Реализовать middleware `Recovery`, логирующий панику как `http_panic`
  со стеком и идентификатором запроса и возвращающий 500; проверить тестом,
  что клиент получает 500, запись со стеком создана и последующий запрос
  обслуживается
- [x] 3.7 Смонтировать цепочку `RequestID → AccessLog → Recovery` в
  `Server.Routes` перед существующими middleware; проверить golden-тестом
  роутов и тестом, что паника даёт две записи (`http_panic` и `http_request`
  со статусом 500)

## 4. Аутентификация, авторизация и изменения прав

- [x] 4.1 Добавить логирование успешного входа (`auth_login`) и выхода
  (`auth_logout`) с идентификатором пользователя и провайдером; проверить
  тестами обработчиков входа и выхода
- [x] 4.2 Добавить логирование неуспешной аутентификации (`auth_failed`,
  уровень `warn`) в обработчик callback провайдера; проверить тестом
- [x] 4.3 Добавить логирование отказа в доступе (`authz_denied`, уровень
  `warn`) с причиной, запрошенным ресурсом и идентификатором пользователя
  в `RequireAuthMiddleware`, `RequireMembershipMiddleware`,
  `RequireSystemAdminMiddleware`; проверить тестами на каждый вид отказа
- [x] 4.4 Добавить логирование изменений прав (`access_changed`): смена роли
  участника, выдача и отзыв прав, изменение административных настроек;
  проверить тестами соответствующих обработчиков
- [x] 4.5 Проверить тестом, что записи об аутентификации содержат числовой
  идентификатор пользователя и не содержат адрес электронной почты
  в открытом виде

## 5. Доменные события

- [x] 5.1 Реализовать в `logging` извлечение полей события рефлексией
  по allowlist типов (`int64`, `*int64`, `[]int64`, `bool`, `time.Time`
  и именованные типы с числовым/булевым представлением), с поддержкой тега
  `log:"safe"` для явно разрешённых строковых полей и кешем дескрипторов
  по `reflect.Type`; проверить unit-тестами на каждый вид поля и на повторный
  вызов из кеша
- [x] 5.2 Реализовать подписчика `SubscribeAll` в режиме `Async`, пишущего
  запись `domain_event` с типом события, извлечёнными полями и контекстом
  из `Meta` (организация, действующий пользователь, команда, период, время
  наступления); зарегистрировать его при сборке приложения и проверить
  тестом, что публикация события порождает запись
- [x] 5.3 Добавить гард-тест: пройти по `event.AllKinds()`, заполнить каждое
  строковое поле уникальным маркером и убедиться, что ни один маркер
  не встречается в записи
- [x] 5.4 Добавить тест, подтверждающий, что новый тип события покрывается
  подписчиком без изменения кода логирования
- [x] 5.5 Перевести существующие записи о потере события в `eventbus`
  на константу `event_dropped` и проверить тестом, что запись содержит тип
  события и причину потери
- [x] 5.6 Проверить тестом, что подписчик логирования не выполняет запросов
  к базе данных

## 6. Внешние обращения

- [x] 6.1 Добавить логирование результата доставки уведомления во внешний
  канал (`external_call`): название канала, исход, длительность, номер
  попытки при повторе; проверить тестами на успех и на ошибку
- [x] 6.2 Проверить тестом, что запись об обращении во внешний канал
  не содержит токен, пароль и адрес подключения с встроенными учётными
  данными

## 7. Перевод вызовов и итоговая проверка

- [x] 7.1 Перевести существующие вызовы логгера в слоях `internal/http`
  и `internal/usecase` на контекстные варианты (`InfoContext`, `WarnContext`,
  `ErrorContext`); проверить тестом, что записи, порождённые обработкой
  запроса, содержат `request_id`
- [x] 7.2 Прогнать полный набор тестов и `go vet`; убедиться, что сборка
  и все тесты зелёные
- [x] 7.3 Запустить приложение локально с `LOG_FORMAT=json`, выполнить
  успешный запрос, запрос с ошибкой 4xx, запрос с ошибкой 5xx и мутацию,
  порождающую доменное событие; убедиться, что все записи — валидный JSON
  с общим `request_id` там, где он применим
- [x] 7.4 Проверить соответствие реализации спецификации
  `specs/observability-logging/spec.md` по каждому requirement и scenario
- [x] 7.5 Зафиксировать в описании развёртывания требуемую настройку сбора
  логов (разбор JSON вместо `grok`) и переменные `LOG_LEVEL`, `LOG_FORMAT`,
  `SERVICE_NAME`, `ENV`

## 8. Исправления по итогам code review

- [x] 8.1 Пересобирать контекст лог-записи о доменном событии из Meta самого
  события: шина сохраняет контекст только первого публикатора батча, из-за чего
  коалесцированные события разных организаций получали дублирующиеся ключи
  и чужие `tenant_id`/`actor_id`; проверить тестом, что батч из событий двух
  организаций не смешивает их идентификаторы и не даёт дублей ключей
- [x] 8.2 Маскировать адрес электронной почты внутри значений строковых
  атрибутов, включая percent-encoded форму: текст ошибки внешнего канала
  содержит путь `/api/v4/users/email/<escaped email>`, и редакция по имени
  ключа его не видит; проверить тестом на прямой и экранированный адрес
- [x] 8.3 Проставлять машиночитаемый код ошибки по статусу, когда обработчик
  ответил напрямую через `http.Error` и код не был записан; проверить тестом,
  что запись о таком ответе содержит `error_code`
- [x] 8.4 Передавать техническую причину в обёртку ответа на 5xx-путях,
  отвечающих через `http.Error` (session-обработчики, OAuth start и callback);
  проверить тестом, что причина попадает в лог и не попадает в тело ответа
- [x] 8.5 Сохранять контекст публикации каждого события при коалесценции в шине,
  вместо переноса `request_id` только на первое событие батча: один запрос может
  опубликовать событие на каждую затронутую сущность (bulk-переход статусов),
  и остальные записи оставались без корреляции с запросом; проверить тестом, что
  все события одного запроса несут его `request_id`, а события разных запросов —
  каждое своё
- [x] 8.6 Выводить диагностику некорректной настройки логирования в обход
  настроенного порога: при `LOG_LEVEL=error` предупреждение о некорректном
  `LOG_FORMAT` отфильтровывалось тем самым уровнем, о соседе которого сообщало;
  проверить тестом, что предупреждение видно при любом допустимом уровне
- [x] 8.7 Сообщать об успешном выходе только после удаления серверной сессии:
  ошибка `Logout` игнорировалась, и запись `auth_logout` утверждала об успехе,
  когда сессия на сервере оставалась живой; проверить тестами на успех и на отказ
- [x] 8.8 Логировать потерю события с контекстом публикации (`WarnContext`):
  запись `event_dropped` теряла связь с запросом, чьё событие не доставлено;
  проверить тестом, что запись несёт `request_id` публикатора
- [x] 8.9 Фиксировать каждое зафиксированное изменение административных настроек
  сразу после его успеха: при отказе последующей записи уже применённые изменения
  (переименование организации, сохранённые настройки) оставались без записи
  `access_changed`; проверить тестом, что отказ на второй настройке не отменяет
  запись о первой
- [x] 8.10 Сообщать о готовности принимать запросы только после успешного
  связывания сокета: запись `app_ready` выводилась до попытки открыть слушатель,
  и при занятом порте инстанс выглядел готовым, ни разу не приняв запрос;
  проверить тестом, что при отказе связывания записи `app_ready` нет
- [x] 8.11 Пробрасывать ошибки обнаружения периодов в исход фоновой задачи:
  `snapshotDuePeriods` превращала отказ хранилища в пустой список, и проход
  снимков сообщал `outcome=ok`, молча пропустив организации; проверить тестом,
  что отказ обнаружения даёт `outcome=failed`
- [x] 8.12 Сохранять организацию и действующего пользователя в записи о панике:
  recovery смонтирован снаружи и видит запрос до обогащения контекста, поэтому
  запись несла только `request_id`; брать накопленный scope из обёртки ответа;
  проверить тестом, что запись о панике несёт `tenant_id` и `actor_id`
- [x] 8.13 Логировать панику и ошибку обработчика событий с контекстом
  публикатора; проверить тестом, что запись о панике подписчика несёт
  `request_id` опубликовавшего запроса
- [x] 8.14 Включать типы потерянных событий в запись о потере при закрытой шине:
  сейчас указывались только подписчик и количество; проверить тестом, что запись
  содержит тип события
- [x] 8.15 Завершить 4.4: добавить запись `access_changed` в обработчики настроек
  обратной связи и health check-in — единственные административные настройки,
  оставшиеся вне аудита; проверить тестами обоих обработчиков
- [x] 8.16 Останавливать приложение при отказе связывания сокета: `app.New` уже
  запустил шину и фоновые задачи, а возврат из этой ветки минует дренаж, из-за
  чего петли продолжали бы работать против закрываемого пула; проверить тестом
  шаг дренажа и привести в соответствие комментарий о чистом выходе
- [x] 8.17 Не выводить в лог непроверенное значение настройки `new_user_policy`:
  оно не валидируется против перечисления, а под общим ключом `value` редакция
  по имени его не маскирует; фиксировать факт изменения настройки без значения
- [x] 8.18 Не допускать адрес получателя в текст ошибки канала: `sender.call`
  вшивал в сообщение путь, а путь адресации по почте содержит сам адрес, поэтому
  редакция по шаблону ловила лишь часть принимаемых форм (короткий TLD,
  интернационализированные адреса); передавать в ошибку метку эндпоинта вместо
  пути и расширить шаблон редакции как второй рубеж; проверить тестами на адрес
  вида `a@b.c` и на нелатинский адрес
- [x] 8.19 Записывать шаблон маршрута вместо конкретного пути: маршрут
  `/invite/{token}` несёт в пути токен приглашения, и запись о запросе клала
  в лог действующие учётные данные; проверить тестом, что токен не попадает
  в запись, а шаблон маршрута попадает
- [x] 8.20 Не завершать ответ успехом после паники, случившейся уже после
  отправки заголовка: 500 дописать нельзя, но ответ финализировался с исходным
  статусом и запись о запросе сообщала об успехе; обрывать поток ответа и
  помечать запись как прерванную; проверить тестом
- [x] 8.21 Переносить организацию в контекст логирования сразу после её резолва,
  до гейта членства: отказ по приостановленной организации не мог её назвать,
  хотя она уже была известна; проверить тестом, что запись `authz_denied`
  содержит `tenant_id`
- [x] 8.22 Использовать шаблон маршрута и в записи о панике: правка 8.19 закрыла
  только access-log, а запись `http_panic` продолжала класть конкретный путь —
  то есть токен приглашения; проверить тестом, что паника на `/invite/{token}`
  не оставляет токен в логе
- [x] 8.23 Использовать шаблон маршрута и в записи об отказе в доступе, вынеся
  правило в один общий пакет: конкретный путь оставался в `authz_denied`, а три
  копии одного правила и привели к тому, что 8.19 закрыла только одну из них;
  проверить тестом, что отказ на маршруте с секретом в пути не оставляет секрет
